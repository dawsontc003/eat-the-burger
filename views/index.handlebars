{{!-- <style media="screen">
    li {
        background-color: wheat;
        margin-bottom: 10px;
        padding: 10px;
        width: 50%;
        font-family: sans-serif;
    }
</style> --}}

{{!-- <h1>Lunches for {{eater}}</h1> --}}

<ul id="notDevoured">
    {{#each burgers }}
    <li>{{burger_name}} <button>Devour</button> </li>
    {{/each}}
</ul>
<ul id="devoured">
    {{#each burgers1 }}
    <li>{{burger_name}} </li>
    {{/each}}
</ul>